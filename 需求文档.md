# 竹谱 需求文档（PRD）

> 版本 v0.6 ｜ 目标读者：实现方（Claude Code）
> v0.2 变更：补齐两首验收曲目、新增 `箫调` 头部字段、新增倚音/延音线(tie)/终止线、
> 明确孔序与三行布局、确定孔位图与水印样式、新增页脚、修正示例 DSL。
> v0.3 变更：`◎` 定为渲染成「闭」；速度拆分为**全曲基准速度**与**曲中变速/渐变**（§3.9）。
> v0.4 变更：变速与段落记号改为**反斜杠命令** + 编辑器命令面板（§3.8.1）；
> 正文里的中文自此只用于歌词；`\fermata` 与变速记号分离存储。
> v0.5 变更：新增**动态谱播放**（§9）。
> v0.6 变更：应用更名为**竹谱**；新增**本地谱库**（§10）与**纯简谱 / 洞洞谱 切换**（§10.4）。
> 外部依赖与待确认项已全部关闭。

---

## 1. 项目目标

做一个**纯前端网页应用**：用户以文本谱（DSL）输入一首曲子，应用自动排版并渲染出：

1. **简谱**（数字谱，带八度点、减时线、附点、延音、休止、升降、圆滑线、延音线、倚音、三连音、反复、歌词等）；
2. **洞洞谱**（每个音符下方对应的箫指法孔位图）；

并支持根据 **筒音作X** 自动推导每个音的指法，最后**导出带水印的图片（PNG / SVG / PDF）**。

谱子存在浏览器本地的**谱库**里（§10），可以按曲目管理、导入导出；
写好的谱子还能**跟练播放**（§9）。整首也可以切成**纯简谱**，不画指法（§10.4）。

第一版乐器：**八孔箫**。架构上要为将来扩展其它乐器（六孔箫、笛等）留出空间——指法数据与渲染逻辑解耦。

### 验收标准

以用户提供的两张原谱图为基准，**两首都要过**：

| 曲目 | 参照图 | 覆盖的特性 |
|---|---|---|
| 《为爱追寻》 | `为爱追寻示例输出图片.png` | 无歌词、大量**延音线(tie)**、附点八分+十六分节奏 |
| 《落了白》 | `落了白示例输出图片.png` | **有歌词**、**倚音**、圆滑线、终止线、页脚 |

- DSL 能完整表达两首曲子；
- 渲染出的简谱在视觉结构上与原图一致（音符、节奏、八度、歌词、弧线等）；
- 渲染出的洞洞谱指法与原图**逐格一致**。

---

## 2. 核心设计原则：单一指法表 + 转调派生

这是整个应用最关键的架构决策，务必按此实现。

**箫的"按法 ↔ 实际音高"是物理固定的；"筒音作X"只改变唱名标签，不改变指法本身。**

因此系统只需要**一张权威指法表**，以「距筒音的半音数」为索引：

```
指法表[半音数 index] → 孔位图（8 个孔的开/闭/半孔状态）
```

- `index = 0` 即筒音（全按）本身；
- `index = 1,2,3,…` 依次向上每升高一个半音的指法；
- 半音音（升降音）对应表中"黑键"位置的 index（用半孔/叉口指法），同样收录在表里。

给定任意简谱音，推导指法只需：**算出它距筒音多少个半音 → 查表**。所有"筒音作X"（作1/作2/作5/作6…）全部自动派生，无需逐一手工录入。详见 §6 算法。

> **`调号:` 不参与指法推导。** 指法只由 `筒音作X` 决定。调号仅用于谱头显示，且可由 `箫调 + 筒音作` 自动推导（§6.5）。

---

## 3. DSL 规范

### 3.1 整体结构

```
<头部：键值对，每行一个>
<空行>
<正文：音符流，空格分音，| 分小节>
```

### 3.2 头部字段

| 字段 | 说明 | 示例 | 必填 |
|---|---|---|---|
| `标题:` | 曲名 | `标题: 落了白` | 是 |
| `副标题:` | 副标题 | `副标题: 箫筒音作2` | 否 |
| `制谱:` | 署名 | `制谱: 示例` | 否 |
| `箫调:` | **新增**。箫的调（筒音作5 时的调），如 `G` / `F` / `D` | `箫调: G` | 是 |
| `筒音作:` | 筒音对应的唱名数字 | `筒音作: 2` | 是 |
| `调号:` | 记为 `1=X` | `调号: 1=C` | 否 |
| `拍号:` | 分数形式 | `拍号: 4/4` | 是 |
| `速度:` | **全曲基准** BPM，整数 | `速度: 66` | 否 |

规则：
- 字段名同时接受中英文冒号（`:` / `：`）。
- **`调号:` 可省略**：省略时由 `箫调` + `筒音作` 自动推导（§6.5）。若填写且与推导结果不符，给出**警告**（不阻断渲染）。
- `箫调` 只影响调号推导与（未来的）音名显示，**不影响指法**。
- **`速度:` 可省略**：省略时谱头**不显示**速度项（两张原图即如此）。曲中变速另见 §3.9。

### 3.3 音符语法

一个音符 = `[升降]数字[八度][附点][歌词]`，各音之间用空格分隔。歌词后缀见 §3.11。

| 元素 | 记法 | 说明 |
|---|---|---|
| 音名 | `1`–`7` | 简谱唱名 |
| 休止符 | `0` | |
| 延音符 | `-` | 独立 token，延长**当前时值层**一拍（见 §3.4） |
| 八度（高） | 后缀 `^` | 中音上方，可叠：`5^` 高八度，`5^^` 更高 |
| 八度（低） | 后缀 `_` | 中音下方，可叠：`5_`、`5__` |
| 附点 | 后缀 `.` | 时值 ×1.5 |
| 延音线 | 后缀 `~` | 与下一个同音相连，后者不重新起吹（见 §3.5） |
| 升 | 前缀 `#` | `#5` |
| 降 | 前缀 `b` | `b5` |
| 还原 | 前缀 `n` | `n5` |
| 歌词 | 音符后紧跟中文字或 `"…"` | `1.隐`、`5"la"`（见 §3.11） |

示例：`#5^.` = 升、高八度、附点的 5；`5_别` = 低八度的 5，歌词"别"。

### 3.4 时值：括号嵌套 = 减时线层数 + 梁分组

- 默认时值 = **四分音符**。
- **小括号 `( )` 每嵌套一层，给括号内所有音加一条减时线**：
  - `(1 2)` → 两个**八分**音符（一条减时线，梁相连）；
  - `((1 2))` → 两个**十六分**；
  - `(((1 2)))` → 三十二分；
- 可混合嵌套：`(1 (2 3))` → 一个八分 `1` + 两个十六分 `2 3`，同处一梁下。
  > **规则就是「嵌套层数 = 减时线条数」，没有例外。** v0.1 此处曾写作 `(1 ((2 3)))`，那是笔误——那样写是三层，得到的是三十二分。
- **括号同时决定梁的分组**：`(1 6_) (5_)` 渲染为「`1 6` 梁相连」+「`5` 单独一条减时线」，与 `(1 6_ 5_)` 三音共梁不同。转录原图时要按图上的梁分组写括号。
- `-`（延音）和 `.`（附点）在括号内**跟随当前括号层的时值**：八分梁里的 `-` 延长一个八分。
- **附点节奏的写法**（原图大量出现的「附点八分 + 十六分」）：`(2. (3))`。写法偏啰嗦，v1 接受现状，后续再考虑简写糖。

### 3.5 圆滑线（slur）与延音线（tie）

两者都渲染为音符上方的弧线，但语义不同，写法也分两种。

**（一）延音线 tie —— 后缀 `~`**

`~` 加在音符后面，表示与**下一个音**相连：

```
3~ 3          → 两个 3 连成一个音
(2. (3~)) (3 6_)   → 十六分的 3 连到下一梁的八分 3
```

- 后一个音**不重新起吹**，其下方**不画指法图**（§7）。
- **可跨梁分组、跨小节**——这正是要用后缀而不是括号的原因：原图里大量的 tie 两端分属不同的减时线梁（如 `2· 3⌒3 6̣`），括号无法正确嵌套表达。
- 两端音高不同时给警告，并退化为圆滑线渲染。

**（二）圆滑线 slur —— 大括号 `{ }`**

```
{5 3 2 1}     → 一条跨四个音的圆滑线
{(6 5)}       → 与时值括号共存
{5 3 | 2 1}   → 可跨小节
```

- 组内每个音**照常画指法图**。
- 便利规则：`{ }` 内**相邻同音**会自动按 tie 处理（等价于 `~`），因为同音相连在乐理上就是延音线。

> 依据：《为爱追寻》全曲弧线均连接同音（`3⌒3`、`2⌒2`、`5⌒5`），图上被连的第二个音一律无指法列；《落了白》弧线连接不同音（`6̣⌒5̣`、`6̣⌒1`），两个音都有指法列。

### 3.6 倚音（装饰音）

用**尖括号 `< >`** 写在主音**之前**：

```
<3 2>3
```

- 渲染：小号数字 + 双减时线（三十二分），带一条向主音的小连线，压在主音左上方（参照《落了白》"奈"字处的 `32⌒3`）。
- 倚音**不占时值**（不参与小节拍数累加）。
- 倚音下方**不画指法图**。
- 后倚音（写在主音之后）第一版不实现，语法预留 `3<2>`。

### 3.7 三连音（tuplet）

用前缀 `3:` + 大括号包裹三个音：

```
3:{1 2 3}
```

- 上方自动标注 `⌐3¬`（三连音记号）。
- 三个音在同一时值单位内均分。默认三连音总时长 = 一拍（四分），如需八分三连音：`(3:{1 2 3})`。
- 架构上预留 `N:{...}` 支持任意连音，第一版只需实现三连音，但解析器按通用 `N:{}` 写。

> 两张参照图均无三连音，此特性按通用乐理实现，暂无逐格验收依据。

### 3.8 小节、反复与段落记号

小节与反复用符号：

| 记法 | 含义 |
|---|---|
| `\|:` | 反复开始 |
| `:\|` | 反复结束 |
| `\|\|` | **终止线**（双细线；曲末必画，见《落了白》结尾） |
| `[1.` … `[2.` | 一房、二房（跳房子），作用到本小节 |

段落记号用**反斜杠命令**（见 §3.8.1）：

| 记法 | 渲染 | 含义 |
|---|---|---|
| `\segno` | `%` | Segno 返回记号 |
| `\coda` | `⊕` | 尾声记号 |
| `\dc` | `D.C.` | Da Capo，从头反复 |
| `\ds` | `D.S.` | Dal Segno |
| `\fine` | `Fine` | 结束 |

- 段落记号**不占时值**。
- **渲染位置分左右**：`\segno` / `\coda` 是跳转目标，画在小节**开头**；
  `\dc` / `\ds` / `\fine` 是读到才执行，画在小节**末尾**。

> 两张参照图均无反复/房子/段落记号，此组按通用乐理实现，暂无逐格验收依据。

### 3.8.1 反斜杠命令与命令面板

**变速与段落记号一律写成 `\name` 形式**，命令表见 `src/core/commands.ts`（分词器、
补全面板、教程三处共用同一份定义）。

设计理由：
1. **正文里的中文一律是歌词**（紧跟在音符后面）。若让中文再兼任记号，
   就会出现「这两个字到底是词还是命令」的歧义。记号全用 ASCII 后，含义唯一。
2. 记号不该靠记忆硬打。编辑器里**输入 `\` 即弹出命令面板**，
   继续打字筛选，`↑` `↓` 选，`Enter` / `Tab` 插入，`Esc` 关闭。
3. 带数值的命令（`\tempo=88`）插入后自动选中数字，可直接改写。

- 命令名**大小写不敏感**。
- 未知命令 → 报错并定位到字符。
- `\tempo` 不带数值 → 报错。
- 旧的中文写法（`渐快` / `渐慢` / `原速` / `延长` / `速度=88`）与
  `rit.` / `accel.` / `a tempo` / `D.C.` / `D.S.` / `%` / `Coda` / `Fine`
  **仍作为兼容别名被接受**，但界面与教程只展示 `\` 命令。

### 3.9 速度：全曲基准 + 曲中变速

速度分两层，互不干扰：

**（一）全曲基准速度** —— 头部 `速度:` 字段（§3.2）。
- 渲染在谱头，形如 `♩=66`；
- **省略则谱头不显示速度项**（两张原图上都没有速度标记）。

**（二）曲中变速** —— 正文里的反斜杠命令（§3.8.1），作用于其**后面的所有音**，直到下一个变速记号：

| 记法 | 渲染 | 含义 |
|---|---|---|
| `\tempo=88` | `♩=88` | 定值变速（突变到新 BPM） |
| `\accel` | `accel.` | 渐快 |
| `\rit` | `rit.` | 渐慢 |
| `\atempo` | `a tempo` | 回到变速前的速度 |
| `\fermata` | 弧线加点 | 自由延长记号（加在**前一个音**上方，不改 BPM） |

- 变速命令**不占时值**，不参与小节拍数累加，可出现在小节内任意位置。
- 渲染位置：其后第一个音符的**正上方**，小号斜体。行末出现的变速记号跟随到下一行行首。
- **一个音只能挂一个变速记号**；连写两个会给警告并只保留后一个。
- **`\fermata` 与变速记号分开存**：延长是加在音符上的演奏法，不是速度指令。
  两者可以落在同一个音上，各占一条竖向带，互不覆盖、互不遮挡。
- `𝄐`（U+1D110）在多数中文字体里没有字形，**延长记号由渲染器自己画**（弧线 + 点），
  不依赖字体。同理 Segno 渲染为 `%` 而非 `𝄋`。

```
// 例：第 1 小节起 rit.，第 2 小节回原速并定到 60，末音自由延长
(2 1) 6_ \rit (2 1) | \atempo 3 5 \tempo=60 3 \fermata - |
```

> 两张参照图均无变速记号，此特性按通用乐理实现，暂无逐格验收依据。

### 3.10 小节与拍数

- `|` 分小节；行尾可不写。
- **允许不完整小节**：首小节（弱起/引子）与「与弱起互补的」末小节可少于拍号。
- 拍数不符一律按**警告**处理（黄色标记），不阻断渲染（见 §4.1）。

> 依据：《落了白》原图开头把「2 拍引子 + 4 拍乐句」挤在一个小节里
> （`0 0 1· 2 1 6̣ 5̣` 共 6 拍，中间**没画小节线**，谱面标 4/4）。
>
> 录入时**已补上这条小节线**做规范化：第 1 小节 = `0 0 |` 两拍弱起，
> 第 2 小节 = `1.隐 (2去) (1了 6_离) 5_别 |` 正好 4 拍，其余 15 小节亦均为 4 拍。
> 这是两首曲子里**与原图唯一的有意差异**，其余一律按图保留。

### 3.10.1 曲中变拍号

谱头的 `拍号:` 是**全曲起点**；前奏与主歌不同拍的曲子，在换拍那一小节的**开头**写
`\meter=4/4`，其后的小节都按新拍号算，直到下一个 `\meter`。

| 记法 | 渲染 | 含义 |
|---|---|---|
| `\meter=3/4` | 小节头上下叠的 `3/4` | 从本小节起换成 3/4 |

- 分母只能是 `2 4 8 16`（简谱的时值体系里没有「三分音符」），写错报错并给出正确写法。
- **时值不受影响**：括号层数决定减时线，四分音符恒等于 1，换拍只改「一小节几拍」。
- **校验按小节各自的拍号算**（§4.1）。没有这条，中途转拍的曲子会把另一种拍号的
  每一小节都标成「拍数不符」，左下角刷屏，真正的笔误全被淹掉。
- **播放的拍点也逐小节算**（§9.2），弱起判定用第一小节自己的拍号。
- 写在小节中间意图含糊，给警告并按整小节生效。
- 谱头那一行仍只显示起始拍号；变化在谱面上就地标出。

```
\meter=2/4 1 2 | 3 4 | \meter=4/4 1 2 3 4 | \meter=3/4 5 3 1 |
```

### 3.11 歌词（内联后缀式）

歌词**直接粘在对应音符后面**，字与音绑定，永不错位（增删音符不影响其它字的对应）。**渲染时仍自动把每个字排到音符正下方**——内联只是输入方式，输出照常上下对齐。

```
0 0 1.隐 (2去) (1了 6_离) (5_别) |
```

规则：
- **中文歌词直接紧跟音符**：音符 token 全为 ASCII，其后紧邻的中文字即该音的歌词，天然可分割。一个音可带多字：`5离别` = 音 `5` + 词 `离别`。
- **拉丁文 / 含空格或标点的词块**用引号包裹：`5"la"`、`3"don't"`。
- **拖腔（一字多音）**：字只写在第一个音上，后续音**不写字**，渲染器自动在其下方画延长线 `——`。
- **休止符 `0`、延音 `-` 不带歌词**（解析器不允许其后附字）。
- **不带歌词的音符**下方留空（或作为拖腔延续上一字，二者渲染一致）。
- **多段歌词**：第 1 段内联；第 2、3 段用独立的 `词2:` / `词3:` 行，按"可唱音先后顺序"对应。第一版实现内联单段即可，`词N:` 预留。

**多段歌词**用 `/` 分段，仍然贴在音符后面：

```
5长/韶 (3亭/光 5) 1^外/逝 - | 6古/留 1^道/无 5边/计 - |
```

- 各段排成上下两行，行首自动标 `1.` `2.`（只有一段时不标）。
- **分隔符在引号外面**：引号保护内容，所以 `1"a/b"` 是一段带斜杠的词，
  英文各段自己加引号即可：`1"la la"/"na na"`。这样「歌词里出现 `/` 怎么办」
  自动有解，不必再发明转义。
- 段数不齐没关系：某个音只写了第一段，第二行那一格就空着；
  想显式跳过某段用空引号 `1""/韶`。
- 列宽取**最长的那一段**，几段词共用同一列；`/` 后面不接词是错误。

### 3.12 注释

以 `//` 起的行为注释，解析时忽略。

---

## 4. 校验规则

解析后、渲染前做校验，**错误在界面上就近标红并给出可读信息**，不阻断其它部分渲染：

1. **每小节拍数校验**：按拍号累加每小节实际时值，标**黄（警告）**并提示实际/应有拍数。
   豁免规则**分方向**，不能一刀切放过首尾小节：

   | 情况 | 处理 |
   |---|---|
   | **超出**拍号（任何小节，含首尾） | **一律警告**。弱起、引子、收尾都只会「少拍」，多出来的拍数一定是写错了 |
   | 首小节**不足** | 豁免（弱起 / 引子） |
   | 末小节**不足**，且首小节也不足 | 豁免（收尾少的那部分正是开头借走的） |
   | 末小节**不足**，但首小节是满拍 | **警告**，并提示「开头不是弱起，结尾通常应当满拍」 |
   | 中间小节不足或超出 | 警告 |

   > 早先的实现一刀切地豁免首尾小节，结果把末小节里真正的笔误也一起咽了
   > （末小节多写 4 拍、少写 1 拍都不报）。
2. 未知 token、括号/大括号/尖括号不匹配、`3:{}` 内音数不为 3 → **报错**定位到字符。
3. 音超出乐器音域（查指法表 index 越界）→ 该音标黄警告（仍渲染简谱，洞洞谱位置留空并提示）。
4. 弧线/反复记号不成对 → 报错。
5. `调号:` 与 `箫调 + 筒音作` 推导结果不符 → 警告。

---

## 5. 简谱渲染要求（第一优先级）

用 **SVG** 渲染（矢量、清晰、便于导出与缩放）。

排版元素：
- 数字主体；
- **八度点**：高音在数字**上方**加点（`^` 数量=点数），低音在**下方**加点；
- **附点**：数字**右侧**加点；
- **减时线**：数字**下方**横线，条数 = 括号嵌套层数；同一梁下的连续音，减时线连成一条；
- **延音符 `-`**：占一个时值位的短横；
- **休止符**：`0`；
- **升降还原**：`♯ ♭ ♮` 置于数字左上；
- **圆滑线 / 延音线**：跨音符的弧线，能跨行时在行末/行首断开续接；
- **倚音**：主音左上方的小号数字 + 双减时线 + 小连线；
- **三连音**：数字上方 `3` + 括弧；
- **变速记号**：`♩=88` / `accel.` / `rit.` / `a tempo` / `𝄐`，小号斜体置于对应音符正上方（弧线层之上，见 §3.9）；
- **小节线** `|`、**终止线** `‖`、**反复线** `‖: :‖`、**房子**方括号；
- **谱头**：
  - **标题**：页面顶部居中，大号粗体；
  - **副标题**：标题正下方，小一号（原图为"箫筒音作2"）；
  - **制谱**：与副标题同一行**右对齐**（原图为"董咚咚（学箫版）制谱"）；
  - **调号 / 拍号 / 速度**：**新增**，谱头显示 `1=C  4/4  ♩=66`。位置在副标题行**左侧**（与副标题、制谱同一行，形成 左-中-右 三段）。两张原图上没有这一项，属本应用新增内容。`速度:` 省略时该项不显示，只出 `1=C  4/4`。
- **页脚**：页面底部居中，`《曲名》 总N页，第M页`（参照《落了白》原图页脚）。

自动换行/分页：一行排满按小节换行，减时线梁与弧线在换行处正确断开；页满自动分页。每行小节数、每页行数**无硬性要求**，由自动排版决定（原图约每行 3–5 小节）。

---

## 6. 转调 / 指法自动推导算法（洞洞谱核心）

### 6.1 唱名 → 半音值

以本调的 `do(1)` 为 0，各唱名相对半音偏移（大调音阶）：

| 唱名 | 1 | 2 | 3 | 4 | 5 | 6 | 7 |
|---|---|---|---|---|---|---|---|
| 半音 | 0 | 2 | 4 | 5 | 7 | 9 | 11 |

- 升 `#` +1，降 `b` −1，还原 `n` 按本位；
- 每高一个八度 +12，每低一个八度 −12。

记某音的绝对半音值：
```
semitone(音) = 基础偏移(唱名) + 升降调整 + 12 × 八度数
```
（八度数：中音=0，`^`=+1、`_`=−1，可叠。）

> 异名同音（如 `b7` 与 `#6`）半音值相同 → 查到同一条指法，无需特殊处理。指法表里用 `♭7` 记法，DSL 里写 `b7` 或 `#6` 都能正确命中。

### 6.2 筒音的半音值

`筒音作X` 表示筒音（全按最低音）的唱名为 **低八度的 X**：
```
semitone(筒音) = 基础偏移(X) − 12
```
> 如个别曲目筒音落在中音，可在头部扩展 `筒音八度:` 字段（预留，第一版默认低八度）。

### 6.3 查表

```
index = semitone(音) − semitone(筒音)
指法 = 指法表[index]
```
- `index < 0` 或 `index ≥ 表长（32）` → 音域越界（见校验 §4.3）。

### 6.4 worked example（筒音作2，已用两张原图逐格验证）

- `筒音作: 2` → `semitone(筒音) = 2 − 12 = −10`
- 低音 `6_`：`semitone = 9 − 12 = −3`，`index = −3 − (−10) = 7` → 表[7] = 音位8 ✓ 与原图一致
- 中音 `1`：`semitone = 0`，`index = 10` → 表[10] = 音位11 ✓
- 中音 `2`：`semitone = 2`，`index = 12` → 表[12] = 音位13 ✓
- 中音 `3`：`semitone = 4`，`index = 14` → 表[14] = 音位15 ✓
- 换成 `筒音作: 5` 时，`semitone(筒音)=7−12=−5`，同一个中音 `5` 变成 `index = 7−(−5)=12`——**指法表不变，只是索引平移**，这正是单表设计的意义。

> 注意：指法表的列名是「音位 N」，从 1 开始；代码中的 `index` 从 0 开始。**`index = 音位号 − 1`**。

### 6.5 调号自动推导

```
筒音音名 = 箫调的 sol           // 例：箫调 G → 筒音 = D；箫调 F → 筒音 = C
调号主音(1) = 筒音音名 − 基础偏移(筒音作X)   // 半音数，mod 12
```
验证：`箫调 G` + `筒音作 2` → 筒音 = D，1 = D − 2 半音 = **C** → `1=C`。
（这也解释了 v0.1 里 `调号: 1=D` 与指法表冲突的问题：G 调箫筒音作 2 时本调是 C。）

### 6.6 指法表数据（已确定，来源：`八孔洞箫指法表.md`）

```jsonc
{
  "instrument": "八孔箫",
  "holeCount": 8,
  "holeOrder": ["第八孔","第七孔","第六孔","第五孔","第四孔","第三孔","第二孔","第一孔"],
  "holeOrderNote": "数组顺序 = 洞洞谱竖排自上而下；第八孔在最上，第一孔在最下",
  "range": { "minIndex": 0, "maxIndex": 31 },   // 共 32 个音位（音位1–音位32）
  "table": [
    { "index": 0, "holes": ["●","●","●","●","●","●","●","●"] }
    // …index 0–31，直接从 八孔洞箫指法表.md 的「音位1–音位32」列转录
  ]
}
```

**符号集（4 种，表中原样保留）**：

| 符号 | 含义 | 渲染 |
|---|---|---|
| `●` | 全闭 | 实心黑圆点 |
| `○` | 全开 | 空心圆（白底黑边） |
| `◐` | 半孔 | 半黑半白圆（左半黑） |
| `◎` | 可开可闭 | **实心黑圆点（按「闭」渲染）** |

> `◎` 定为渲染成「闭」，与两张原图一致（音位8 / 音位11 的第二孔等处，原图均画成闭孔，逐格核对无例外）。
> 实现上仍保留配置项 `可开可闭渲染: 闭 | 开`，**默认 `闭`**。

- 解析 `八孔洞箫指法表.md` 时按行取「第八孔 → 第一孔」，按列取「音位1 → 音位32」，转置成 `table[index].holes`。
- 表中的「G调音名 / F调音名 / 筒音作N」各行**不参与计算**，可忽略（调号由 §6.5 推导）。
- **同一唱名不同八度自然对应不同 index → 不同指法**，无需特殊处理。

---

## 7. 洞洞谱渲染要求

**三行布局（自上而下）**：

```
第 1 行：简谱（数字 + 减时线 + 八度点 + 弧线）
第 2 行：歌词（无歌词时整行省略，见《为爱追寻》）
第 3 行：洞洞谱（竖排孔位图）
```

> 注意顺序：歌词在**简谱与洞洞谱之间**（v0.1 写反了）。三行随小节整体换行，逐音符左右对齐。

**孔位图样式**（参照两张原图）：

- 每个**实音符**下方画一个**竖长条**：
  - 底色 **橙色 `#F9B556`**，圆角矩形；
  - **顶部格**：该音的唱名数字（**斜体**，带八度点，如 `6̣`、`5̣`），黑色；
  - 其下 **8 个圆**，自上而下 = **第八孔 → 第一孔**；
  - **分组分隔线**：在第 4 个圆与第 5 个圆之间、第 7 个圆与第 8 个圆之间各留一条细白缝（即 `第八七六五 | 第四三二 | 第一`）。
- **不画孔位图的情况**（下方留空）：
  - 休止符 `0`；
  - 延音符 `-`；
  - **被延音线(tie)连到的第二个及之后的同音**（§3.5）；
  - 倚音（§3.6）；
  - 音域越界的音（同时标黄警告）。
- 页面底色 `#FEFEFC`。

> 原图的"闭孔"用的是作者的熊猫头像图标；**第一版统一用普通实心黑圆点**，不做头像。

---

## 8. 导出

- 支持 **PNG / SVG / PDF** 三种。
- **水印**：导出时弹窗让用户填水印文字；
  - 留空 = 无水印；
  - 有文字 = 平铺**斜向重复**半透明文字，覆盖整页。
  - **样式固定不可调**：颜色 `#E6E6E6`，倾角约 45°，大号字，铺满页面，置于内容下层。
- 导出包含谱头（标题/副标题/制谱/调号/拍号/速度）与页脚（`《曲名》 总N页，第M页`）。
- PNG 支持选择倍率（1x/2x/3x）保证清晰。

---

## 9. 动态谱播放（跟练）

顶栏「▶ 播放」把静态谱变成跟练用的动态谱：**上半谱面一个红框罩住当前音、
下半洞洞谱从右往左滑**，中间那张即当前指法。

**上半只排简谱与歌词，不画洞洞谱**（`layout(score, key, { omitFingering: true })`）：
下面的条子已经是洞洞谱了，上面再来一遍纯属重复，而那条带占掉的竖向空间
正是一屏能看几行的关键。横向宽度仍按有洞洞谱算，所以折行位置和打印谱完全一致。

### 9.1 开播前的体检

- **有错误**：按钮置灰（时值都算不准，播出来没有意义）；
- **有警告**：先弹确认，列出警告条目，可以「仍然播放」或「返回修改」。

### 9.2 时间轴（`src/core/playback.ts`，纯函数）

1. **反复展开**：`|:` `:|` 与房子号 `[1. [2.` 按实际演奏顺序铺平；没写 `|:` 时按惯例从曲子开头回跳。
   写了三房就走三遍（遍数由段内最大的房子号决定）。
   **遍数在段与段之间会归位**：走完一组房子号即视为这一段反复结束——
   少了这一步，第二段反复会带着上一段的遍数开场，它的一房被整段跳掉，直接从二房进。
   **D.C. / D.S. / Coda / Fine 不参与播放**——它们的组合语义太多（al Coda、al Fine…），猜错比不做更误导。
   因此靠 `\ds` 跳回去才到达的房子（如三房写在 D.S. 之后）播放时到不了。
2. **合并起吹单位**：延音横线 `-`、延音线 `~`、相邻同音并进前一个音——吹的时候本来就是一口气，
   洞洞谱不换卡片，上方的方框横向拉长罩住整段。
3. **落地速度**：
   - 基准 BPM 取谱头 `速度:`，没写则用默认值（播放窗里可当场改，会盖过谱头）；
   - `\tempo=N` 从该音起突变；
   - `\accel` / `\rit` 在「记号到下一个变速记号」之间**几何插值**渐变到 1±g 倍，
     g 默认 0.3、播放窗里可调（谱面只写了记号，播放必须给出具体数字，这是唯一的「发明」）；
   - `\atempo` 回到上一个变速记号生效前的速度；
   - `\fermata` 按二倍时值延长。
4. **节拍点**：**逐小节**数，拍位在小节内从 0 开始——这样曲中变拍号（§3.10.1）不会把
   整条拍位网格顶歪，「第 0 拍 = 重拍」也直接成立。唯一的例外是**弱起的第一小节**
   （引子那几拍是从上一小节借来的，敲成重拍会让整首强弱错开一格）。

### 9.3 声音（WebAudio 现场合成，无音频素材）

- **音高**：`lookupFingering` 给的 index 就是「距筒音的半音数」，筒音音名由箫调决定
  （G 调箫筒音 = D4，F 调箫 = C4），两者一合即绝对频率。
- **音色**：基频正弦 + 弱二次/三次谐波 + 一路带通白噪当气声 + 约 60ms 软起音 + 延迟进入的轻微颤音。
- **节拍器**：重拍高、弱拍低；**预备拍**在开播前空打一小节并在画面上倒数。
- 三者都可单独开关。

### 9.4 洞洞谱条的手感（几条都是试出来的）

- **停住 → 滑走 → 停住**，不是匀速漂。线性进度在快曲子上整条一直在动，眼睛锁不住中间那张；
  现在每个音的绝大部分时间静止，只在末尾约 0.16 秒（短音按比例压缩，最多占本音时值的 45%）
  用 smoothstep 滑向下一张。
- **中间大、两边一样大**：只有中心那张放大，其余一律同一尺寸，不做「越远越小」的递减。
- **不做虚化**：全部不透明。滑出条子边缘是被裁掉，而不是淡出。
- 铺几张由条子实测宽度决定，宽屏就多铺几张。
- **尺寸一律按比例、不写死像素**：条子占播放窗高度的固定一成数，卡片高度取条子高度的固定比例，
  卡片宽度与间距再由洞洞谱一列自身的长宽比反推。写死像素的话，页面一放大，
  条子按 CSS 像素照常占位、可视高度却变小了，上下的比例就跑了。

### 9.5 实现上的三条铁律

1. **时钟以音频为准**：画面每帧从 `AudioContext.currentTime` 反推位置。
   rAF 在后台标签页会停，两个时钟各走各的必然漂开，声画就对不上了。
2. **排期不挂在 rAF 上**：用定时器（约 120ms 一次）+ **两秒提前量**把音符排进音频时钟。
   切到后台时 rAF 被停掉，若排期跟着停，已排的那点提前量放完就静音；
   切回来又会把这期间早该响的音一次性补排，全挤在一起炸响。
   定时器在后台只是被压慢到约 1 秒一次，提前量比它大就断不了。
   同时，**时间已经过去的音一律丢弃**，不夹到「现在」补响——那正是炸响的来源。
3. **谱面 SVG 不参与每帧重绘**：方框是绝对定位的 div，按页面元素实测尺寸换算；
   洞洞谱卡片 memo 到 step 粒度。整页上百个孔的 svg 只在开窗时渲染一次。

### 9.6 操作

进度条可拖到任意位置开始；空格播放/暂停，←→ 各跳 5 秒，Esc 关闭；倍速 0.5x–1.5x。

---

## 10. 谱库与文件

### 10.1 存储

谱子存在 IndexedDB（库名 `zhupu`，对象仓库 `scores`），一条记录：

```ts
{ id, title, dsl, mode: 'xiao' | 'jianpu', createdAt, updatedAt }
```

- **标题不单独维护**，它就是谱头里的「标题」字段；改名 = 重写那一行。
- `title` 在记录里冗余存一份，列表页就不必把每首谱都解析一遍。
- 编辑时**防抖 700ms 自动存盘**。写谱是反复打磨的活，要求用户记得按保存是不合理的。
- 纯逻辑（序列化 / 解析 / 取标题 / 撞 id）与 IndexedDB 调用分开写，前者有单测。

### 10.2 页面与路由

两个页面，自己解析 hash，不引路由库：

| 路径 | 页面 |
|---|---|
| `#/library` | 谱库：列表、新建、改名、复制、删除、导入、导出全部 |
| `#/edit/<id>` | 编辑器 |

空 hash 进站时，上次打开的谱子还在就接着写，否则去谱库。
之所以要真路由而不是弹窗：前进/后退、刷新回到原处、地址能收藏，这三件事弹窗做不到。

### 10.3 导入导出

- **单首 = DSL 原文 `.txt`**。文件格式就是编辑器里那段字：人能读、能发给别人、能塞进 git，
  不另造一套 JSON 包装。导入时一次可选多个文件。
- **整库备份 = JSON**（`format: "zhupu-library"`），因为它要带上标题、模式、时间这些库内元数据。
  解析容错从宽：只要认得出 `dsl` 就收，缺的字段就地补——备份是用户自己手里的东西，
  宁可收下也别整份拒绝。导入撞 id 一律换新 id，绝不覆盖库里已有的谱子。
- 浏览器清站点数据会连 IndexedDB 一起清掉，谱库页明写一句「重要的谱子记得导出备份」。

### 10.4 纯简谱 / 洞洞谱

编辑器顶栏的开关，存在谱子记录的 `mode` 上（这是谱子自身的属性，不是界面偏好）。

- **洞洞谱**：现状，简谱 + 歌词 + 每个音的指法列。
- **纯简谱**：`layout(..., { omitFingering: true })`，不排指法那一带；
  谱头表单收起「箫调 / 筒音作」（它们只为指法服务），调号改为直接填。
- **播放跟着这个开关走**：洞洞谱模式是滑动指法条 + 合成箫音；
  纯简谱模式只有谱面 + 方框，音色换成合成钢琴。
- **音高口径也跟着变**：箫模式按「距筒音多少半音」算，筒音音名由箫调定（G 调箫筒音 = D4）；
  简谱模式与乐器无关，主音直接取调号，中音 1 落在 C4~B4 这个八度。
- **校验也分口径**（`validate(score, { forXiao })`）：纯简谱模式下 箫调 / 筒音作 只剩
  「调号没写时用来推导」这一个用处，因此**跳过音域校验与调号一致性校验**——
  谱面上根本没有箫，拿箫的音域和指法推导去卡一份简谱是没有道理的。
  只有「调号没写、箫调又认不出来」时才提示，因为那时确实没有调号可显示。

---

## 11. 技术建议（实现方可调整）

- **构建**：Vite + TypeScript。
- **UI**：React（左侧 DSL 编辑器 + 右侧实时预览的双栏布局）。
- **渲染**：核心为**纯函数管线** `DSL文本 → AST → 布局模型 → SVG`，与 UI 框架解耦，便于单元测试。
- **导出**：SVG 直接下载；PNG 用 canvas 光栅化；PDF 用 svg→pdf（如 `svg2pdf.js` + `jsPDF`）。
- **无后端**，全部浏览器内完成。
- **测试**：对解析器与转调算法写单测；以《为爱追寻》《落了白》为集成测试快照。

---

## 12. 里程碑

| 阶段 | 内容 | 交付 |
|---|---|---|
| **M1** | DSL 解析器（头部+音符+时值+八度+升降+休止+弧线+倚音+三连音+反复/房子+变速+歌词）、简谱 SVG 渲染、歌词对齐、小节校验 | 输入两首 DSL，右侧出正确简谱，与原图结构对齐 |
| **M2** | 指法表转录 + 转调查表算法 + 洞洞谱渲染 + 三行对齐 | 洞洞谱与两张原图逐格一致 |
| **M3** | 导出 PNG/SVG/PDF + 水印弹窗 + 页脚分页 | 可导出带/不带水印图片 |
| **M4** | 打磨：弧线跨行、D.C./D.S./Coda、多段歌词、音域越界处理、样式贴合原图 | 达到验收标准 |
| **M5** | 动态谱播放（§9）：时间轴 + 滑动洞洞谱 + 合成箫音与节拍器 | 点播放能跟着吹完一首 |
| **M6** | 本地谱库（§10）：IndexedDB + 导入导出 + 路由；纯简谱模式与钢琴音 | 谱子存得住、管得了，简谱用户也能用 |

依赖：指法表数值**已齐备**（`八孔洞箫指法表.md`），M2 不再有外部阻塞。

---

## 13. 验收样例

### 13.1 《落了白》（筒音作2）

头部与开头 3 小节（已对照原图逐音核对）：

```
标题: 落了白
副标题: 箫筒音作2
制谱: 示例
箫调: G
筒音作: 2
拍号: 4/4

// 原图未画这条小节线，此处补上（见 §3.10）
0 0 |
1.隐 (2去) (1了 6_离) 5_别 |
(3_提 5_笔) (3却 2难) 1写 - |
(2如 1霜) 6_雪 (2化 1开) (6_时 5_最) |
```

其余小节（含"奈"字处的倚音 `<3 2>3`、结尾终止线）见 `src/samples/reference.ts`，全 17 小节已逐音录入。

### 13.2 《为爱追寻》（筒音作2）

头部与开头 2 小节：

```
标题: 为爱追寻
副标题: 箫筒音作2
制谱: 示例
箫调: G
筒音作: 2
拍号: 4/4

0 0 0 (6_ 1) |
(2. (3~)) (3 6_) (1. (2~)) (2 3) |
```

> 第 2 小节里两处 `~` 为同音延音线 → 第二个音不画指法列（与原图一致）。
> 全 9 小节见 `src/samples/reference.ts`。

### 13.3 两首曲子的指法列数（逐格验收基线）

《为爱追寻》各小节应画的指法列数，已对照原图逐格点数，并固化为单测：

```
[2, 6, 6, 6, 5, 5, 6, 6, 2]
```

---

## 14. 待办 / 依赖清单

- ✅ 八孔箫权威指法表 —— `/home/yuanr/flute-tab-converter/八孔洞箫指法表.md`（32 个音位，含半孔/可开可闭）
- ✅ 《为爱追寻》原谱图 —— `为爱追寻示例输出图片.png`（本地保留，未入库）
- ✅ 《落了白》原谱图 —— `落了白示例输出图片.png`（本地保留，未入库）

> 两张原谱图是第三方制谱作品，**不随仓库分发**（已在 `.gitignore` 里排除）。
> 录入与核对时放在仓库根目录即可；代码与测试都不读取它们。
- ✅ `◎`（可开可闭）→ 渲染成「闭」（§6.6）
- ✅ 速度 → 拆为全曲基准 `速度:` + 曲中变速记号（§3.9）；两首曲子原图未标 BPM，**暂缺不显示**，后续用户补充即可
- ✅ 两首曲子完整 DSL 已对照原图逐音录入（`src/samples/reference.ts`），**音高/节奏待用户过目确认**

**M1 + M2 已实现**（解析器 / 简谱 SVG / 歌词对齐 / 校验 / 指法表 / 转调查表 / 洞洞谱 / 三行对齐）。
下一步 M3：导出 PNG / SVG / PDF + 水印弹窗。
